// Supabase has been removed. This module survives only as a typed null
// stub so existing imports keep compiling. All auth/data flows now run
// against the in-memory dev path (devMega session, memory repositories).
export const isSupabaseClientConfigured = false;
export const supabase = null;
