# Copy this file to apps/api/.env.local and paste your server-only values.
# The Supabase backend was removed; the API runs entirely on in-memory
# repositories until a new backend is wired in. No env vars are required.
