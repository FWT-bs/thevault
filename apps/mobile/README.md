# Mobile App Workspace Stub

This repository currently keeps the Expo app at the repo root so existing local workflows stay intact.

`apps/mobile` is reserved for a future physical move of:

- `app/`
- `components/`
- `constants/`
- `assets/`

Until that migration lands, root scripts (`npm run start`, `npm run ios`, etc.) remain canonical.
